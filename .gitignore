node_modules
dist
.DS_Store
*.local
.env
.env.*
# Almanac never ships exports — but if you back up locally, don't commit it.
*.almanac.json
.vercel
# Playwright
/test-results
/playwright-report
/playwright/.cache
