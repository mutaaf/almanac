name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Cancel in-flight runs for the same branch so the latest commit's results
# are the ones we look at.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-build:
    name: Typecheck + build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build

  e2e:
    # Both chromium and mobile-webkit are GATING checks. The WebKit-specific
    # timing issues that previously forced `continue-on-error: true` on the
    # mobile-webkit job were closed by ticket 0005 (compose() route-awaits +
    # the waitForDb helper). Mobile Safari is where most users actually open
    # the app — its tests block merge too.
    name: E2E (${{ matrix.project }})
    runs-on: ubuntu-latest
    needs: typecheck-build
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        project: [chromium, mobile-webkit]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      # Cache Playwright browser binaries between runs. Keyed on the
      # @playwright/test version pulled in by package-lock.json so a Playwright
      # bump always re-installs. (We previously only ran `install` on a cache
      # MISS, which meant a stale-cache run skipped the browser install and
      # left the webkit binary missing; promoting mobile-webkit to gating
      # exposes that.)
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      # Always run `install` — it's a no-op if the binary is already on disk,
      # and a genuine install otherwise. `install-deps` covers the OS libs.
      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.project == 'mobile-webkit' && 'webkit' || matrix.project }}

      - name: Run tests
        run: npx playwright test --project=${{ matrix.project }} --reporter=github,html

      - name: Upload Playwright trace + report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.project }}
          path: |
            playwright-report
            test-results
          retention-days: 14
